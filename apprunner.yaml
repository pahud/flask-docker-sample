version: 1.0
runtime: python3
build:
  env:
    - name: PLATFORM
      value: "AWS App Runner"
  commands:
    build:
    - pip install -r requirements.txt
run:
  command: python app.py
  network:
    port: 80
